/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add this to allow your local IP
  allowedDevOrigins: ['127.0.0.1'],

  // ... your other config options (if any)
};

module.exports = nextConfig;
